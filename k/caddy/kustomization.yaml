#ManagedBy: cue

patches:
  - path: patch-service-tailscale.yaml
namespace: caddy
resources:
  - resource-namespace-caddy.yaml
helmCharts:
  - releaseName: caddy
    namespace: caddy
    valuesInline:
      https:
        enabled: true
      config:
        caddyFile: |-
          :443 {
            handle / {
             respond "hello"
            }
          }
    name: caddy
    version: 0.2.4
    repo: https://charts.alekc.dev
    includeCRDs: true
