#ManagedBy: cue

patches:
  - path: patch-service-tailscale.yaml
namespace: caddy
resources:
  - resource-namespace-caddy.yaml
helmCharts:
  - releaseName: caddy
    namespace: caddy
    valuesInline:
      config:
        caddyFile: |-
          :8080 {
            handle / {
             respond "hello"
            }
          }
    name: caddy
    version: 0.2.4
    repo: https://charts.alekc.dev
    includeCRDs: true
